<h1><%= @user.name %></h1>

<p><%= link_to "View as JSON", user_path(@user, format: 'json') %></p>

<ul>
  <% @posts.each do |post| %>
  <!-- closed <li> tag -->
  <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>
